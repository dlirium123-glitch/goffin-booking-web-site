name: Outlook → Firestore (freeSlots sync)

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *" # toutes les 30 minutes

concurrency:
  group: outlook-sync
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (with cache)
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: tools/outlook-sync/package-lock.json

      - name: Google Auth (service account)
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

      - name: Install deps
        working-directory: tools/outlook-sync
        run: npm ci

      - name: Validate secrets (no output)
        env:
          OUTLOOK_ICS_URL: ${{ secrets.OUTLOOK_ICS_URL }}
        run: |
          if [ -z "$OUTLOOK_ICS_URL" ]; then
            echo "❌ Missing secret: OUTLOOK_ICS_URL"
            exit 1
          fi
          echo "✅ OUTLOOK_ICS_URL present"

      - name: Run sync
        working-directory: tools/outlook-sync
        env:
          FIREBASE_PROJECT_ID: "goffin-booking-web"
          OUTLOOK_ICS_URL: "${{ secrets.OUTLOOK_ICS_URL }}"
          DAYS_FORWARD: "90"
          TZ: "Europe/Brussels"
          DEBUG_OUTLOOK_META: "1"
        run: node sync.js

      - name: Summary
        if: always()
        run: |
          echo "### 🔄 Outlook → Firestore sync" >> $GITHUB_STEP_SUMMARY
          echo "- Project: goffin-booking-web" >> $GITHUB_STEP_SUMMARY
          echo "- Frequency: every 30 minutes" >> $GITHUB_STEP_SUMMARY
          echo "- DAYS_FORWARD: 90" >> $GITHUB_STEP_SUMMARY
          echo "- TZ: Europe/Brussels" >> $GITHUB_STEP_SUMMARY
          echo "- DEBUG_OUTLOOK_META: 1" >> $GITHUB_STEP_SUMMARY
